---
title: Installation
layout: default
parent: ComfyUI
nav_order: 1
---

# Installing ComfyUI

A child page sits under its section by setting `parent:` to the section's title
and giving it a `nav_order`. Replace this stub with the real steps.

{: .note }
> Just the Docs ships callouts: `.note`, `.warning`, `.important`, `.highlight`,
> `.new`.

```bash
git clone https://github.com/comfyanonymous/ComfyUI.git
cd ComfyUI
pip install -r requirements.txt
```
